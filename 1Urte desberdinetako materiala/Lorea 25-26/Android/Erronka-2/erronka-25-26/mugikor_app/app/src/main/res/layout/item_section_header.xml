<?xml version="1.0" encoding="utf-8"?>
<!-- Layout: item_section_header.xml -> HomeAdapter.SectionVH (MainActivity-ko home zerrendako atal-burua) -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="6dp"
    android:paddingBottom="8dp">

    <!-- ATALAREN IZENA (Pelikulak/Antzerkiak/Kontzertuak...) -->
    <TextView
        android:id="@+id/tvSection"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="18sp"
        android:textColor="#111827"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- EKINTZA: “Guztiak ikusi” (atal osoa irekitzeko) -->
    <TextView
        android:id="@+id/tvSeeAll"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/section_guztiakIkusi"
        android:textSize="13sp"
        android:textColor="#2563EB"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBaseline_toBaselineOf="@id/tvSection"/>
</androidx.constraintlayout.widget.ConstraintLayout>