package com.example.mvc_linearlayout.model

// MODEL: datu-logika kudeatzen du (kalkuluak)
class CalculatorModel {

    // Bi zenbaki batu
    fun addNumbers(a: Double, b: Double): Double {
        return a + b
    }
}